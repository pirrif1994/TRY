class Profile(object):
	def __init__(self,user_id,first_name,last_name,access_token)
		self.user_id = user_id
		self.first_name = first_name
		self.last_name = last_name
		self.access_token = acess_token
